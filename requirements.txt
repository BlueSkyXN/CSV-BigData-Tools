# Core dependencies
pandas>=1.3.0
tqdm>=4.60.0

# Optional dependencies for advanced features
# For Dask-based sorting (install with: pip install -r requirements.txt dask[complete] psutil)
# dask[complete]>=2021.0.0
# psutil>=5.8.0

# For Spark-based sorting (install with: pip install -r requirements.txt pyspark)
# pyspark>=3.0.0